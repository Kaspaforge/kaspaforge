# kaspaforge.org — the canonical origin of Kaspa Forge.
# Legacy hosts (safe./escrow.officeforge.co) serve their own robots.txt with their own
# sitemaps — kept so Google reads them and discovers the 301s to this domain.
User-agent: *
Allow: /
# Per-vault management panel is a key-gated private interface, not content.
Disallow: /manage.html
Disallow: /ru/manage.html

Sitemap: https://kaspaforge.org/sitemap-forge.xml
